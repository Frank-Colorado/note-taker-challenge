const fs = require("fs");
const path = require("path");

const getNotes = (req, res) => {};

const createNote = (req, res) => {};

const deleteNote = (req, res) => {};

module.exports = {
  getNotes,
  createNote,
  deleteNote,
};
